/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */
package estructuras_control;

/**
 *
 * @author USER
 */
public class Estructuras_control {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        /*estructura control
        while
        do while
        for
        foreach- recorre en base a un objeto, necesita tener una lista
        un string no es un tipo de dato primitivo esta compuesto por char-> con una sola comilla ya que java es sensible 
        ejemplo, podemos poner una contraseña o un nombre cada caracter tiene una posicion que empieza en 0 
        .Length (python solo len) a veces como atributo o como funcion
        en funcion ocupa () 
        sintaxis del while:
        """""""while (prueba logica)con los referencias o verdadera o falsa empieza el loop cuando es verdadera
        bloque de codigo - true        |while (True) 
        sale hasta que sea falso       | sout("holaa!");te crasheas la memoria y la pc
                                          Break;          
        ex if(pais.equals("HN")||(pais.equals("CR")||(pais.equals("GT"))
            Sout(´CentroAmerica´);
        stwitch (pais){ 
        Case "HN": 
        Case "CR":
        case "GT":
        Sout("CA")
        break;
        }
        """""""Do while"""" (entra al menos una vez al ciclo)bloque de cofigo true 
        do{
        bloque de codigo - true
        Sout("hola");
        } while (prueba logica); debe de evaluarse para entrar, si cumple o no si es verdadero entra, si no, no     
        """""for(Inicio; prueba logica; incremento/decremento)                  loop  IDvar = 0 <-fuera del loop
        Int Index es i IDvar (inicializacion con valor); DVAR OR <>>=<= Expresion; IDVAR ++1 +=acumulador (IDvar = IDvar +10) -- quita 1
        */
        /*int Contador=0;
        //   estaba en 0 y se le va agregando 
         while(Contador<13)
         {
             System.out.println( "5 x" + Contador+ "="+ (5*Contador));
             Contador++;
             
         }*/
        
        
        
        
        
    }
    
}
